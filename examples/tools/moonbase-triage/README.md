# Moonbase Triage

Moonbase Triage is a deterministic, offline example for Vorton Tools. It sorts synthetic lunar-base incidents using fixed urgency and impact rules.

It is not installed by default. The example makes no network requests, reads no installation data, and performs no mutations. Its manifest declares these boundaries so a future Tool host can evaluate the same contract before installation.
