# Organization-owned Fly deployment intent. No deployment is performed.
app = "moonbase-lab-vorton"
primary_region = "sea"

[build]
  image = "REPLACE_WITH_LOCKED_OCI_REFERENCE"

[env]
  VORTON_INSTALLATION = "moonbase-lab"
